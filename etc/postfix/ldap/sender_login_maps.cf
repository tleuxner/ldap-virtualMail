server_host = ldap://ldap.example.com
search_base = ou=Mail,dc=example,dc=com
version = 3
start_tls = yes
bind = yes
bind_dn = cn=mail,ou=Services,dc=example,dc=com
bind_pw = secret 
query_filter = (|(mailAlias=%s)(mailDrop=%s))
result_attribute = mailDrop 
